name: Test Attestation Service
description: Test Attestation Service
runs:
  using: "composite"
  steps:
    - run: cd /packages/attestation-service/
      shell: bash
    - run: npm test
      shell: bash